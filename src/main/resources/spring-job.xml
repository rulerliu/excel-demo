<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
       xmlns:reg="http://www.dangdang.com/schema/ddframe/reg"
       xmlns:job="http://www.dangdang.com/schema/ddframe/job"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd http://www.dangdang.com/schema/ddframe/reg
       http://www.dangdang.com/schema/ddframe/reg/reg.xsd
                        http://www.dangdang.com/schema/ddframe/job
                        http://www.dangdang.com/schema/ddframe/job/job.xsd">

    <reg:zookeeper id="regCenter"
                   server-lists="${job.zkServerList}"
                   namespace="${job.namespace}"
                   base-sleep-time-milliseconds="${job.baseSleepTimeMilliseconds}"
                   max-sleep-time-milliseconds="${job.maxSleepTimeMilliseconds}"
                   max-retries="${job.maxRetries}"
                   session-timeout-milliseconds="${job.sessionTimeoutMilliseconds}"
                   connection-timeout-milliseconds="${job.connectionTimeoutMilliseconds}" />



</beans>